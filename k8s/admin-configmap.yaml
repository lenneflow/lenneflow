kind: ConfigMap
apiVersion: v1
metadata:
  name: admin-service
data:
  spring.cloud.kubernetes.discovery.all-namespaces: "true"
  application.yml: |-
    spring:
     cloud:
      kubernetes:
        discovery:
          all-namespaces: true
          service-labels:
            spring-boot: true