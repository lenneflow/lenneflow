apiVersion: v1
kind: Service
metadata:
  name: account-service
  labels:
    app: account-service
    spring-boot: "true"
spec:
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: account-service
  type: ClusterIP